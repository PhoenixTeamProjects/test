/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['localhost', 'www.huanyukuntai.com', 'huanyukuntai.com'],
    unoptimized: true,
  },
}

module.exports = nextConfig
